import React from 'react';
import { Modal } from 'antd';

const MovieForm = () => {
  return (
    <Modal centered open={true} width={100} footer={null}>
      This is Movie Form
    </Modal>
  );
};

export default MovieForm;
